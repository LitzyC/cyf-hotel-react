import Profile from "./Profile.js";

const SearchResults = {};

const handleShowProfile = id => {
  console.log(id);
};

<table>
  <th>
    <button onClick={() => handleShowProfile(el.id)}>Show Profile</button>
  </th>
</table>;

return <div />;
